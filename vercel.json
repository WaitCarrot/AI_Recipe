{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python"
            "config": {
                "timeout": 60
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.py"
        }
    ]
}
